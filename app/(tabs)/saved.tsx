import { View, Text } from "react-native";

// TODO: rename it to bookmarks
export default function Saved() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-3xl text-dark-200 font-bold">Saved</Text>
    </View>
  );
}
